networks:
  proxy:
    external: true

services:
  nginx:
    image: nginx:1.31.2@sha256:f15d93384bc7066ba635474370a566c7c1e10a21fd5baf8d30f36cee6f8ae08f
    networks:
      - proxy
    environment:
      PUID: 1000
      PGID: 1000
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.nginx.middlewares=crowdsec-traefik-bouncer,auth"
      - "traefik.http.routers.nginx.rule=Host(`${SERVICE_SUBDOMAIN:-nginx}.${SERVICES_BASE_DOMAIN}`)"
      - "traefik.http.routers.nginx.tls=true"
      - "traefik.http.services.nginx.loadbalancer.server.port=80"
    restart: unless-stopped
