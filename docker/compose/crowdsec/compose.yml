networks:
  proxy:
    external: true
  crowdsec:
    name: crowdsec

services:
  crowdsec:
    image: crowdsecurity/crowdsec:v1.7.6@sha256:63b595fef92de1778573b375897a45dd226637ee9a3d3db9f57ac7355c369493
    environment:
      PUID: 1000
      PGID: 1000
      GID: "${GID-1000}"
      COLLECTIONS: "crowdsecurity/linux crowdsecurity/traefik firix/authentik"
    volumes:
      - ../traefik/data/logs:/var/log/traefik/:ro
      - ./data/config:/etc/crowdsec/
      - ./data/db:/var/lib/crowdsec/data/
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - crowdsec
    restart: unless-stopped

  crowdsec-traefik-bouncer:
    image: fbonalair/traefik-crowdsec-bouncer:0.5.0@sha256:e3e8f33923d2871069de08dd2931a9eb6c6760f76d6b9e3229d3b9c84211dd94
    environment:
      PUID: 1000
      PGID: 1000
      CROWDSEC_BOUNCER_API_KEY: ${CROWDSEC_BOUNCER_API_KEY}
      CROWDSEC_AGENT_HOST: crowdsec:8080
      GIN_MODE: release
    networks:
      - proxy
      - crowdsec
    depends_on:
      - crowdsec
    restart: unless-stopped

  crowdsec-dashboard:
    image: ${GIT_SUBDOMAIN:-git}.${SERVICES_BASE_DOMAIN}/${GITEA_USER}/crowdsec-dashboard:latest
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      MB_DB_FILE: /data/metabase.db
      MGID: "${GID-1000}"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.crowdsec.middlewares=crowdsec-traefik-bouncer,auth"
      - "traefik.http.routers.crowdsec.rule=Host(`${SERVICE_SUBDOMAIN:-security}.${SERVICES_BASE_DOMAIN}`)"
      - "traefik.http.routers.crowdsec.tls=true"
      - "traefik.http.services.crowdsec.loadbalancer.server.port=3000"
    depends_on:
      - 'crowdsec'
    volumes:
      - ./data/db:/metabase-data/
    networks:
      proxy:
      crowdsec:
