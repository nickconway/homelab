services:
  nebula-sync:
    image: ghcr.io/lovelaze/nebula-sync:latest
    container_name: nebula-sync
    environment:
    - FULL_SYNC=true
    - RUN_GRAVITY=true
    - CRON=0 * * * *
    env_file:
      - .env
    restart: unless-stopped
